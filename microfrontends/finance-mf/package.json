{
  "name": "@munchbox/finance-mf",
  "version": "0.1.0",
  "private": true,
  "description": "FinanceApp micro-frontend — exposes FinanceApp for the shell to load at runtime. See README.md.",
  "scripts": {
    "dev": "vite --port 5104",
    "build": "vite build",
    "preview": "vite preview --port 5104"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@originjs/vite-plugin-federation": "^1.3.6",
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.2.0"
  }
}
