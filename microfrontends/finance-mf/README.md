# finance-mf (stub — not yet scaffolded as a running app)

Would expose `FinanceApp`: the Ledger, Finance overview, and Payments screens currently
in `admin/src/pages/Admin/{Ledger,Finance,Payments}.jsx`. Talks to `finance-service` and
`payment-service` via the gateway.

Reserved dev port: **5104**. Build using `orders-mf` as the template — see
`../README.md`'s "Building a new module" section.
