# delivery-mf (stub — not yet scaffolded as a running app)

Would expose `DeliveryApp`: the Delivery partners list + Live map currently in
`admin/src/pages/Admin/{DeliveryAccounts,LiveMap}.jsx`. Talks to `delivery-service` and
`identity-service` via the gateway.

Reserved dev port: **5103**. Build using `orders-mf` as the template — see
`../README.md`'s "Building a new module" section.
