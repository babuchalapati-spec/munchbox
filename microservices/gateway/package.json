{
  "name": "@munchbox/gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Munchbox API gateway — single entry point for all client apps, routes by path prefix to each backend service.",
  "main": "src/index.js",
  "scripts": {
    "dev": "node src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "http-proxy-middleware": "^2.0.6",
    "jsonwebtoken": "^9.0.2"
  }
}
